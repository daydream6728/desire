# TODO

> actually i want this feature removed altogether, revert the memory as github issues on the memory repo.
> keep the diff minimal wrt toumix/desire. it should be ready for next run

- [x] restore every prompt file, `config.env`, `sweep.py` and the hook to `453462e`,
      the fork's state before the project work
- [x] `CHANGELOG.md` keeps both entries and records the revert — a rule that was
      tried and dropped is what that file is for
- [ ] MEMORY_REPO: the Action, the item template and the issue-shaped conventions out,
      `README.md` the live board again, carrying what the items hold — its own PR
- [ ] close the items, they are not the memory any more
