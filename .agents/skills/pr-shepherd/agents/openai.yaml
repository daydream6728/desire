interface:
  display_name: "PR Shepherd"
  short_description: "Watch this PR until merge or close"
  default_prompt: "Use $pr-shepherd to keep watching this pull request until it merges or closes."

policy:
  allow_implicit_invocation: true
