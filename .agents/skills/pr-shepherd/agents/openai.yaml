interface:
  display_name: "PR Shepherd"
  short_description: "Watch this PR until it merges"
  default_prompt: "Use $pr-shepherd to keep watching this pull request until it merges."

policy:
  allow_implicit_invocation: true
